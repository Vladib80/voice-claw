# VoiceClaw environment variables

# ── Required ──────────────────────────────────────────────────────────────

# Server signing key for bridge HMAC tokens (like JWT_SECRET).
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Without this, bridge tokens break on every deploy.
BRIDGE_SECRET=change-me

# ── Optional ──────────────────────────────────────────────────────────────

# Admin API access token (for /api/admin/* endpoints)
# VOICECLAW_ADMIN_TOKEN=change-me

# Deployed server URL for CORS (e.g. https://yourapp.onrender.com)
# ALLOWED_ORIGIN=https://yourapp.onrender.com

# Fallback voice API keys — only used if user hasn't provided their own
# OPENAI_API_KEY=sk-proj-...
# GROQ_API_KEY=gsk_...
